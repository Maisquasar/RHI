#version 450

layout(local_size_x = 64) in;

layout(set = 0, binding = 0) buffer Data {
    float values[];
} data;

void main() {
    uint id = gl_GlobalInvocationID.x;
    data.values[id] *= 2.0;
}
